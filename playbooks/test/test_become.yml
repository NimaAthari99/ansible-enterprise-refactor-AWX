---
- name: Test Become
  hosts: wordpress-server-1

  become: true
  become_method: sudo

  tasks:

    - name: Check effective user
      ansible.builtin.command: whoami
      register: result

    - name: Show result
      ansible.builtin.debug:
        var: result.stdout