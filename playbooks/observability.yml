---
- name: Configure observability clients
  hosts: "{{ target_hosts | default('observability_clients') }}"
  gather_facts: true
  become: true
  serial: "{{ rollout_batch_size | default('20%') }}"
  roles:
    - role: nima.platform.observability_agent
      tags: [observability]
