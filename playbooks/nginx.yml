---
- name: Configure Nginx hosts
  hosts: "{{ target_hosts | default('nginx_servers') }}"
  gather_facts: true
  become: true
  serial: "{{ rollout_batch_size | default('20%') }}"
  roles:
    - role: nima.platform.nginx_setup
      tags: [nginx]
