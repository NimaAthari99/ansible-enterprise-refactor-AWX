# nginx_setup

Installs and configures Nginx. Use it as `nima.platform.nginx_setup`.

Secrets such as `nginx_setup_auth_password` are intentionally empty by default and must be injected from Ansible Vault, an AWX credential, or another secret backend.
