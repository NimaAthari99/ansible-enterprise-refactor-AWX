---
- name: Reload systemd
  become: true
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart Docker
  become: true
  ansible.builtin.service:
    name: docker
    state: restarted
