# SSH Bootstrap

Performs serial SSH migration. Hard-coded credential URL was replaced with a variable and TLS verification defaults to enabled.

Use via the FQCN `nima.platform.ssh_bootstrap`. Environment-specific values belong in the control repository inventory, not in this role.
