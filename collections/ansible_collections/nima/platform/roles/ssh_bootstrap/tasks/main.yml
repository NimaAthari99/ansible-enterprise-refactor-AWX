---
- name: Configure resolver
  ansible.builtin.import_tasks: resolvedconf.yml
  tags: [ssh_bootstrap, dns]

- name: Configure resolver
  ansible.builtin.import_tasks: ssh.yml
  tags: [ssh_bootstrap, ssh]

