# Privileged Access

Creates validated sudoers fragments for bootstrap users.

Use via the FQCN `nima.platform.privileged_access`. Environment-specific values belong in the control repository inventory, not in this role.
