---
# Match the awx.awx collection to the exact AWX development commit in use.
# Install this into the project-local ./collections path so it wins collection
# resolution over stale system/user copies.
collections:
  - name: git+https://github.com/ansible/awx.git#/awx_collection/
    type: git
    version: c0aedc6e37f453b885879717ca066397309e1c83
