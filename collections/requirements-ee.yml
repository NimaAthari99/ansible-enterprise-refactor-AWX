---
collections:
  - name: community.general
    version: "==13.3.0"

  - name: ansible.posix
    version: "==2.2.2"

  # AWX itself is a development build based on c0aedc6e3. Keep the controller
  # collection on the same commit so AWX jobs do not fall back to the older
  # released collection that POSTs to the removed /api/v2/tokens/ endpoint.
  - name: git+https://github.com/ansible/awx.git#/awx_collection/
    type: git
    version: c0aedc6e37f453b885879717ca066397309e1c83
