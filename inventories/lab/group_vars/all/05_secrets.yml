---
# Secret values are intentionally NOT stored here.
# AWX should inject secret_* variables with a Custom Credential or encrypted extra vars.
platform_http_proxy: "{{ secret_platform_http_proxy | default('') }}"
